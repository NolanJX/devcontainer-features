{
    "name": "Claude Code CLI",
    "id": "claude-code",
    "version": "1.4.0",
    "description": "Installs the Claude Code CLI via the official standalone installer",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "stable"
            ],
            "default": "latest",
            "description": "Release channel ('latest' or 'stable') or a specific version (e.g. '2.1.212')."
        },
        "mattpocock-skills": {
            "type": "boolean",
            "default": false,
            "description": "Pre-install Matt Pocock's Skills. Requires setting CLAUDE_CODE_PLUGIN_SEED_DIR at runtime — see NOTES.md."
        },
        "superpowers": {
            "type": "boolean",
            "default": false,
            "description": "Pre-install Superpowers. Requires setting CLAUDE_CODE_PLUGIN_SEED_DIR at runtime — see NOTES.md."
        }
    }
}
