{
    "name": "Claude Code CLI",
    "id": "claude-code",
    "version": "1.1.0",
    "description": "Installs the Claude Code CLI globally via npm",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "stable"
            ],
            "default": "latest",
            "description": "Version of @anthropic-ai/claude-code to install: a dist-tag (e.g. 'latest'), a specific version (e.g. '2.1.212'), or a version range (e.g. '~2.1.0')."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/node:2"
    ]
}
